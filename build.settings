settings = {
	orientation = {
	--	Default orientation (portait, portraitUpsideDown, landscapeLeft, landscapeRight)
		default = 'portrait',

	--	Locked orientation (iOS)
		content = 'portrait',

	--	Allowed orientations
		supported = { 'portrait', 'portraitUpsideDown' }
	},
}